namespace ChallengeHub;

public class Palindrome
{
    
    /// <summary>
    /// Escreva uma função que verifique se uma string é um palíndromo (lê-se da mesma forma de trás para frente).
    /// Exemplo: IsPalindrome("racecar") retorna true, IsPalindrome("hello") retorna false.
    /// </summary>
    /// <param name="str"></param>
    /// <returns></returns>
    public static bool IsPalindrome(string str)
    {
        Dictionary<char, string> = new Dictionary<char, string>();
    }
}